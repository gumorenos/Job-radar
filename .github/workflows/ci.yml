name: CI

on:
  push:
    branches:
      - main
      - feat/phase-2-core-foundation
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_DB: job_radar
          POSTGRES_USER: job_radar
          POSTGRES_PASSWORD: job_radar_ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U job_radar -d job_radar"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    env:
      JOB_RADAR_APP_ENV: test
      JOB_RADAR_DATABASE_URL: postgresql+psycopg://job_radar:job_radar_ci@127.0.0.1:5432/job_radar
      JOB_RADAR_API_KEY: ci-only-test-key
      JOB_RADAR_STORAGE_PATH: /tmp/job-radar-ci-storage

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: true
          python-version: "3.14"

      - name: Sync locked environment
        run: uv sync --locked --dev

      # Temporary per-file diagnostics for PR #39. Remove before merge.
      - name: Ruff diagnostic - API
        continue-on-error: true
        run: uv run --locked ruff check app/api/v1/evidence.py app/api/v1/router.py

      - name: Ruff diagnostic - models
        continue-on-error: true
        run: uv run --locked ruff check app/db/enums.py app/db/models/__init__.py app/db/models/evidence.py

      - name: Ruff diagnostic - evidence domain
        continue-on-error: true
        run: uv run --locked ruff check app/domains/evidence

      - name: Ruff diagnostic - evidence tests
        continue-on-error: true
        run: uv run --locked ruff check tests/unit/test_evidence_schemas.py tests/integration/test_evidence_api.py

      - name: Ruff
        run: uv run --locked ruff check .

      - name: Mypy
        run: uv run --locked mypy app tests

      - name: Frontend JavaScript syntax
        run: |
          for file in app/web/*.js; do
            node --check "$file"
          done

      - name: Deployment shell syntax
        run: >-
          bash -n
          ops/preflight.sh
          ops/deploy.sh
          ops/backup.sh
          ops/verify_backup.sh
          ops/smoke.sh
          ops/install_openclaw_bridge.sh
          ops/enable_openclaw_bridge.sh
          ops/uninstall_openclaw_bridge.sh

      - name: OpenClaw bridge syntax on host Python
        run: /usr/bin/python3 -m py_compile ops/openclaw_job_radar_sync.py

      - name: Production Compose config
        run: docker compose --env-file .env.production.example config >/dev/null

      - name: Docker image build
        run: docker build -t job-radar:ci .

      - name: Unit tests
        run: uv run --locked pytest -q tests/unit

      - name: Alembic upgrade
        run: uv run --locked alembic upgrade head

      - name: Integration tests
        run: uv run --locked pytest -q tests/integration
